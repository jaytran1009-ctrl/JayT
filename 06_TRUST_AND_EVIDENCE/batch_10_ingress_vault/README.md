# Batch 10 ingress vault

This directory accepts only the two evidence files authorized by JAYT-322 for each scoped slot: `<SLOT>.raw.html` and `<SLOT>.headers.json`. It is empty until an operator captures real bytes. Do not place screenshots, synthetic HTML, cookies, authorization tokens, or unredacted headers here.
