
      .apex-header {
        height: auto;
        min-height: 54px;
        padding: 8px 10px;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        gap: 8px;
      }
      .apex-brand-text small { display: none; }
      .apex-header-controls { gap: 6px; }
      .apex-header-btn {
        min-height: 44px;
        min-width: 44px;
        padding: 0 10px;
        font-size: 11px;
      }
      .apex-zone-select {
        min-height: 44px;
        padding: 0 8px;
        font-size: 11px;
        max-width: 150px;
      }
      .apex-workspace {
        padding: 8px 10px 68px;
        gap: 10px;
      }
      .apex-hub-switcher {
        width: 100%;
        justify-content: center;
        padding: 4px;
        gap: 6px;
      }
      .apex-hub-btn {
        min-height: 44px;
        min-width: 44px;
        padding: 0 12px;
        font-size: 12px;
      }
      .apex-brief-banner {
        padding: 8px 10px;
        gap: 6px;
        border-radius: 12px;
      }
      .apex-brief-meta { gap: 6px; font-size: 10.5px; }
      .apex-brief-tag { padding: 2px 6px; }
      .apex-hero-decision {
        padding: 10px 12px;
        gap: 8px;
        border-radius: 14px;
      }
      .apex-hero-quick-row {
        gap: 6px;
        flex-wrap: nowrap;
        overflow-x: auto;
        padding-bottom: 4px;
        scrollbar-width: none;
      }
      .apex-hero-quick-row::-webkit-scrollbar { display: none; }
      .apex-toggle-pill {
        min-height: 44px;
        min-width: 44px;
        padding: 0 10px;
        font-size: 11px;
        white-space: nowrap;
        flex-shrink: 0;
      }
      .apex-refine-box {
        padding: 4px 10px;
        max-height: 52px;
        margin-top: 2px;
      }
      .apex-refine-summary {
        min-height: 44px;
        font-size: 11.5px;
      }
      .apex-hero-rec-box {
        padding: 10px 12px;
        gap: 8px;
        border-radius: 14px;
      }
      .apex-rec-header { gap: 6px; }
      .apex-rec-title { font-size: 13.5px; }
      .apex-rec-clock-tag { font-size: 10.5px; }
      .apex-rec-cards-grid { grid-template-columns: 1fr; gap: 8px; }
      .apex-rec-card {
        padding: 12px;
        gap: 6px;
        border-radius: 12px;
      }
      .apex-rec-tag { font-size: 9.5px; padding: 2px 5px; }
      .apex-rec-deal-name { font-size: 13.5px; }
      .apex-rec-price { font-size: 16px; }
      .apex-rec-reason { font-size: 11px; line-height: 1.3; }
      .apex-rec-disclosure { font-size: 10px; line-height: 1.25; padding-top: 4px; }
      .apex-card-actions {
        flex-direction: row;
        gap: 6px;
        margin-top: 6px;
      }
      .apex-cta-btn {
        min-height: 44px;
        min-width: 44px;
        font-size: 12px;
      }
      .apex-slots-grid { grid-template-columns: 1fr; gap: 10px; }
      .apex-slot-card {
        padding: 12px;
        gap: 8px;
        border-radius: 14px;
      }
      .apex-slot-header {
        min-height: 44px;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }
      .apex-matrix-grid { grid-template-columns: 1fr; gap: 8px; }
      .apex-matrix-select { min-height: 44px; }
      .apex-mobile-nav-bar { display: flex; }
      .apex-mobile-nav-item {
        min-height: 44px;
        min-width: 44px;
        font-size: 11px;
        padding: 4px 6px;
      }
    