# JAYT-319 — B04_10 One-Shot Capture Work Order

Capture exactly one unauthenticated raw response from the scoped CDC leaf page; redact sensitive headers and validate SHA-256 plus the pre-declared span. No public approval or render is granted. Any future card must retain the mandatory health disclaimer and must not provide medical advice, booking or treatment direction.
