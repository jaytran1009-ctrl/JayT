# JAYT-330-R7 — DIRECT HUMAN ATTESTATION & ZERO-SYNTHETIC GOVERNANCE WORK ORDER

**Issued:** 2026-09-07T05:52:31.4924900Z  
**Scope:** Commercial Staging `http://127.0.0.1:4176/` only  
**Production authority:** NONE

## Absolute prohibitions

1. An agent, script, runner or model must not invent a person's name, employee ID, email, spoken transcript, approval decision or signature.
2. An unkeyed SHA-256 over a constructed string is not a digital signature and must not be described as one.
3. Expected screen-reader output must not be copied into an `observed` field.
4. Historical R6 artifacts must remain preserved but marked non-authoritative by the R6 forensic invalidation.

## Lead Operator gate

1. A real person must start Narrator or NVDA and operate the visible browser session.
2. The operator must directly provide the observed transcript or Speech Viewer export after the test. The evidence must contain the current catalog values; Antigravity may compare it but must not author it on the operator's behalf.
3. Minimum sampled values must include the exact current catalog values for B14_JB_70145 (157,000 VND), B14_JB_70144 (80,000 VND), B14_PL_DTX64GB (290,000 VND) and B14_GALAXY_DANANG_TARIFF (from 45,000 VND), plus all 22 link names and reading order.
4. The operator must attest in their own message or approved signing system. A local script cannot generate the attestation.

## Product and Data & Trust gates

1. Send the policy hash and TTL matrix to the actual authorized representatives.
2. Each representative must return their decision through a user-controlled channel or approved signing system.
3. Record the received event verbatim, including source/channel identifier and timestamp. Do not invent employee records when no real organizational directory is available.
4. Until both decisions are received, policy status remains `PROPOSED_NOT_ENFORCEABLE`.

## QA closeout

1. Preserve the accepted real-browser zoom evidence and all valid automated gates.
2. Validate new human evidence against the immutable 22-card catalog and reject any value mismatch.
3. Enforce temporal consistency: no receipt may be generated before the latest evidence event it claims to certify.
4. Emit `STAGING_BATCH_14_R7_DIRECT_HUMAN_ATTESTATION_RECEIPT.json` only after all real human inputs exist.

## Exit gate

R7 closes only upon direct, attributable human evidence that was not generated by Antigravity or a script. Production `v3.422.0` and the commercial Staging catalog remain frozen; no commercial deploy, affiliate, tracking or automatic publication is authorized.
